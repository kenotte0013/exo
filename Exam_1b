#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Datenais{
  int Jour;
  int Mois;
  int Annee;
}Datenais, s1, s2;

struct Ouvrier{
  char Nom[30];
  char Prenom[30];
  struct Datenais Date;
}ouv1, ouv2;
int idnum;
int choix = 0;
int main()
{
printf("que voulez vous faire? \n");
printf(" 1 : editer un ouvrier \n 2 : Afficher un ouvrier \n");
scanf("%d", &choix);
switch (choix)
{
    case 1 :
    printf("Entrer le Nom et Prenom de l'ouvrier\n");
    scanf("%s %s", ouv1.Nom, ouv1.Prenom);
    break;
    
    case 2 :
    printf("vous avez tape 2");
    break;
    
    default:
    printf("choix incorrect");
}

printf("Nouvelle Saisie ou consulter la database? \n");
printf(" 1 : editer un ouvrier \n 2 : Afficher un ouvrier \n");
scanf("%d", &choix);
switch (choix)
{
    case 1 :
    printf("Entrer le Nom et Prenom de l'ouvrier\n");
    scanf("%s %s", ouv1.Nom, ouv1.Prenom);
    break;
    
    case 2 :
    Printf("entrer le ou les numéro ID");
    scanf()
    For (I = idnum)
    printf("%s %s", ouv1.Prenom, ouv1.Nom);
    break;
    
    default:
    printf("choix incorrect");
}
}
